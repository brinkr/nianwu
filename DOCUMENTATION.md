# 念物 (Nian Wu) —— 数字化断舍离设计白皮书

> “物有始终，念有回响。”

## 1. 设计初衷 (Design Philosophy)

**念物** 是一款旨在解决现代人“囤积症”与“情感羁绊”冲突的数字化工具。

我们发现，人们之所以不舍得扔东西，往往不是因为物品本身的价值，而是因为物品承载了不可复制的**记忆碎片**。当我们将物品丢弃时，潜意识里害怕的是记忆的丢失。

**核心逻辑：**
通过“数字化归档”+“人工智能感应”+“告别仪式感”，将物理空间释放，同时将情感价值转化为永久的数字存证。

---

## 2. 核心功能模块

### 2.1 拾遗 (The Scan)
用户通过相机捕捉物品的多维细节。我们设计了两种截然不同的“归档仪式”：
- **羁绊 (Sentiment Mode)**：针对带有回忆的旧物（如初恋的信、旧吉他）。AI 会化身为物品的灵魂，以第一人称撰写告别信。
- **尘俗 (Utility Mode)**：针对纯功能性杂物（如过期药、快递盒）。AI 会化身为严谨的档案馆员，出具理性的处分回执，确认其无残留价值。

### 2.2 仪式 (The Ritual)
这是应用最核心的交互点。我们没有使用简单的“点击保存”，而是设计了**沉浸式长按释怀**：
- **星轨逻辑**：在“羁绊”模式下，长按时会出现旋转的琥珀色星轨。
- **触感反馈**：模拟心理上的“放下”过程，只有当仪式完成，物品才会进入念物馆。

### 2.3 念物馆 (The Gallery)
- **追忆画廊**：采用瀑布流布局，展示那些温润的情感旧物。
- **封存档案**：采用紧凑的档案卡片布局，记录那些被理性清理的杂物。

---

## 3. UI/UX 演化过程

### V1: 工具原型期
最初，这只是一个简单的图片列表。背景是标准的白色，交互逻辑生硬，缺乏情感厚度。

### V2: 意境营造期
- **引入“暖纸”色调 (`#f2f0e9`)**：取代冷淡的纯白，营造出宣纸或老照片的质感。
- **加入“墨石”按钮**：通过渐变和滤镜，模拟一方浸润在水中的砚台。

### V3: 仪式感重塑 (当前版本)
- **暗房取景**：拍摄界面切换为深色模式（Stone-900），让用户像在暗房中审视底片一样审视自己的物品。
- **动态呼吸感**：主页加入流动的 ZenBackground 渐变球体，象征着空间的流动与循环。
- **长按交互优化**：解决了移动端触控热区过小的问题，引入了全局事件捕获，确保告别仪式的丝滑体验。

---

## 4. 技术实现要点

- **AI 引擎**：使用 **Gemini 2.5 Flash** 模型。
  - 针对不同模式，我们编写了高度差异化的 `systemInstruction`。
  - 通过 `responseSchema` 强制要求 AI 返回包含体积估算 (`estimatedVolume`) 和情感关键词 (`sentiment`) 的结构化 JSON。
- **前端架构**：
  - **React 19** + **Tailwind CSS**。
  - **Canvas-free Animation**：主要使用 CSS 关键帧动画和 Tailwind 扩展，确保在低端设备上的流畅性。
  - **Local Storage**：所有记忆都保存在用户本地，确保私密性。
- **数据管理**：提供“记忆结集”与“时光回溯”功能，允许用户导出/导入 JSON 备份。

---

## 5. 展望

“念物”不仅是一个清理空间的工具，它更像是一个**电子祭坛**。未来，我们计划引入：
1. **ASMR 声音仪式**：在告别时加入碎纸、焚烧或风铃的声音。
2. **AR 展示**：在空旷的房间里，通过 AR 重新看见那些已经“离去”的物品。

---

*“放下，是为了更好地拿起。”*